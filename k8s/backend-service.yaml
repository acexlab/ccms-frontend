apiVersion: v1
kind: Service
metadata:
  name: ccms-backend-service
  namespace: ccms
  labels:
    app: ccms-backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: ccms-backend
